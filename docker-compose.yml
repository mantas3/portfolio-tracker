version: "3.8"

services:
  portfolio-ledger:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio_ledger_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
